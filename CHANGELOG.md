# 更新记录

## v1.6.0 - 2026-08-29

### 新增

- 新增「模型」可用性面板（宽屏管理页）：按站点真实流量查看各模型的成功率、平均延迟、平均 TPS 和健康状态。
- 支持切换 24 / 72 / 168 小时统计窗口、按模型名搜索、只看有流量的模型。
- 概览表格以站点完整模型列表为基准左连接性能数据，未产生流量的模型也会列出。
- 点击表格行展开该模型明细与走势迷你图，再次点击同一行收起。
- 访问令牌、登录 Cookie 和 Agent Router 三种鉴权模式均支持读取模型指标；Cookie 与 Agent Router 模式在站点标签页内发起同源请求。
- 站点不提供 `/api/perf-metrics` 时，自动降级为聚合个人调用日志（`/api/log/self`）生成同样的指标视图。
- 平台卡片上的站点网址改为超链接，点击可在浏览器新标签页打开。

### 优化

- 模型表格数值列表头与数据列对齐方式统一，修复列错位。
- 展开明细改为局部更新行状态，不再整表重建，避免滚动位置跳动。
- 关闭模型面板时清空明细内容，重新打开不会残留上一次的数据。
- 读取指标时先尝试匿名请求，仅在返回 401 时才换取会话令牌重试，并对令牌做缓存与并发去重。
- 去掉模型面板脚注中关于 RPM 的说明文字。

### 兼容性

- 模型面板全程只读：不调用模型、不消耗额度、不触发签到，原有签到链路未做改动。
- 「模型」按钮只在宽屏管理页出现，侧边栏窄屏视图不显示。
- 日志降级模式最多采样最近 600 条调用记录，且只反映当前账号自己的调用。
- 每分钟请求数（RPM）上游接口未对普通用户开放，面板不展示该项。
- 升级本版本不需要迁移原有平台配置。

## v1.4.0 - 2026-08-23

### 更新

- 更新适配了一些中转站。
- Agent Router 签到后自动完成退出、GitHub OAuth 重新登录、额度激活和临时标签页清理。
- 优化登录失效、安全验证页和非 JSON 响应的识别及处理。

## v1.3.0 - 2026-08-22

### 新增

- 支持签到接口启用 Cloudflare Turnstile 的 NewAPI 站点。
- 检测到「Turnstile token 为空」时，自动打开目标站点并显示官方验证控件。
- 用户完成 Turnstile 验证后，自动携带一次性 token 重新提交签到。
- 支持自定义每日签到时间，默认时间为 08:01。
- 浏览器在设定时间未运行时，当天再次启动浏览器会自动补检。
- 定时补检会先查询当天签到状态，只处理尚未签到的平台。
- 增加签到确认通知，用户可以选择「允许签到」或「跳过今天」。
- 增加「自动允许一键签到」开关，可跳过每日确认直接执行。

### 优化

- 插件为 Turnstile 验证临时创建的标签页，会在验证并提交签到后自动关闭。
- 「签到成功」「今日已签到」或其他提交结果均会正确清理临时验证标签页。
- 用户原本已经打开的目标站点标签页不会被自动关闭。
- 定时签到由固定小时循环改为每日指定时间检查。

### 兼容性

- Turnstile 验证必须在目标站点页面环境内完成；需要人工交互时无法完全无人值守。
- 原有平台、令牌和 Cookie 模式配置保持兼容。
- 旧版定时签到设置升级后默认使用每天 08:01，自动允许默认关闭。

## v1.2.0 - 2026-08-21

### 新增

- 增加 Google Chrome 浏览器支持，Chrome 和 Edge 现在可以使用同一份扩展源码。
- 新版 Chrome / Edge 点击工具栏图标时使用原生侧边栏。
- 对不支持 `sidePanel` API 的旧版 Chromium 浏览器增加宽屏管理页降级入口。
- README 增加 Chrome 安装步骤和浏览器版本建议。

### 修复

- 修复 Cookie 鉴权模式仍被前端强制要求填写访问令牌的问题。
- 修复包含“今日”等字样的失败消息可能被误判为“今日已签到”的问题。
- 修复签到成功后，本月签到次数和累计额度没有及时更新的问题。
- 当签到 POST 响应不包含统计数据时，自动追加只读统计请求以补齐数据。
- 修复浅色主题下操作结果提示框文字与背景颜色过于接近、内容难以辨认的问题。

### 兼容性

- 建议使用 Chrome 116+ 或 Edge 116+。
- 配置数据继续保存在浏览器本地，升级到本版本不需要迁移原有平台配置。
## 1.5.0

- 新增 Agent Router GitHub 重登录签到模式。
- 按网站当前实现调用退出、OAuth state 和 GitHub 登录回调，由登录响应的 `checked_in` 判断签到结果。
- 改进非 JSON、登录跳转和安全验证响应的错误提示。
- 批量签到改为并行执行，移除按站点串行等待逻辑。
- Agent Router 退出并启动 GitHub 重登录后不再立即刷新额度，避免登录切换期间误报安全验证页面。
- Agent Router 同源请求会等待页面加载完成，并在收到 HTML 登录页或安全验证页时稳定页面上下文后重试一次。
- 删除 Agent Router 签到前错误的令牌 `/api/user/self` 请求，避免被重定向到首页后中断流程。
- Agent Router 额度刷新改为在页面主环境中使用与网站 Axios 一致的 XHR 请求；接口异常时使用最近一次 OAuth 登录数据降级显示。
- 兼容账户接口的 `quota`、`available_quota`、`remaining_quota` 等额度字段。
- 修复 Agent Router 令牌模式额度刷新仍走 Service Worker 直连的问题，账户与日志接口统一改为站点页面同源请求。
- 账户接口失败时不再仅因日志统计成功而静默显示空额度。
